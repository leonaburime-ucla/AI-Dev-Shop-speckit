# Memory

Toolkit-owned memory system definitions and architecture notes.

This folder does not hold live project memory. Runtime memory files live in the workspace surface at `<ADS_PROJECT_KNOWLEDGE_ROOT>/memory/`, mirrored in-repo under `project-knowledge/memory/`.
